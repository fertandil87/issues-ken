<h1>UserAnswer list</h1>

<table class="records_list">
    <thead>
        <tr>
            <th>Id</th>
            <th>Answer</th>
            <th>Responsetime</th>
            <th>Rightanswer</th>
            <th>Date</th>
            <th>Actions</th>
        </tr>
    </thead>
    <tbody>
    {% for entity in entities %}
        <tr>
            <td><a href="{{ path('administrator_show', { 'id': entity.id }) }}">{{ entity.id }}</a></td>
            <td>{{ entity.answer }}</td>
            <td>{{ entity.responseTime }}</td>
            <td>{{ entity.rightAnswer }}</td>
            <td>{% if entity.date %}{{ entity.date|date('Y-m-d H:i:s') }}{% endif %}</td>
            <td>
                <ul>
                    <li>
                        <a href="{{ path('administrator_show', { 'id': entity.id }) }}">show</a>
                    </li>
                    <li>
                        <a href="{{ path('administrator_edit', { 'id': entity.id }) }}">edit</a>
                    </li>
                </ul>
            </td>
        </tr>
    {% endfor %}
    </tbody>
</table>

<ul>
    <li>
        <a href="{{ path('administrator_new') }}">
            Create a new entry
        </a>
    </li>
</ul>
